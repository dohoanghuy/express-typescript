describe("Sample test", () => {
  it("should return result success when add two numbers", () => {
    expect(1 + 1).toEqual(2);
  });

  it("should return result success when subtract two numbers", () => {
    expect(1 - 1).toEqual(0);
  });

  it("should return result success when multi two numbers", () => {
    expect(1 * 1).toEqual(1);
  });

  it("should return result success when divide two numbers", () => {
    expect(1 / 2).toEqual(0.5);
  });
});
